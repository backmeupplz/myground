services:
  jellyfin:
    image: jellyfin/jellyfin:latest
    container_name: myground-jellyfin
    restart: unless-stopped
    ports:
      - "${BIND_IP}:${EXIT_PORT}:8096"
    volumes:
      - ${STORAGE_config}:/config
      - ${MEDIA_PATH}:/media:ro
    healthcheck:
      test: curl -f http://localhost:8096/health || exit 1
      interval: 3s
      timeout: 3s
      retries: 30
      start_period: 5s
    environment:
      JELLYFIN_PublishedServerUrl: "http://localhost:${EXIT_PORT}"

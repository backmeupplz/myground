[metadata]
name = "Immich"
description = "High-performance self-hosted photo and video management — a Google Photos replacement"
icon = "image"
website = "https://immich.app"
category = "photos"
gpu_services = ["immich-server", "immich-machine-learning"]

[[install_variables]]
key = "IMMICH_DB_PASSWORD"
label = "Database password"
input_type = "password"
required = true

[defaults]
IMMICH_PORT = "8083"
IMMICH_DB_PASSWORD = "myground-immich-default"

[health]
port = 8083
path = "/api/server/ping"
interval_secs = 30

[[storage.volumes]]
name = "upload"
container_path = "/usr/src/app/upload"
description = "Uploaded photos and videos"

[[storage.volumes]]
name = "ml_cache"
container_path = "/cache"
description = "Machine learning model cache"

[[storage.volumes]]
name = "db_data"
container_path = "/var/lib/postgresql/data"
description = "PostgreSQL database files"

[storage.volumes.db_dump]
container = "myground-immich-db"
command = "pg_dumpall -U postgres"
dump_file = "immich_db_dump.sql"
restore_command = "psql -U postgres < /tmp/immich_db_dump.sql"
wipe_command = "psql -U postgres -c 'DROP DATABASE IF EXISTS immich;'"
